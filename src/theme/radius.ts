import {miniUnits} from './utils';

export const radius = Object.freeze({
  xxxLarge: miniUnits(3),
  xxLarge: miniUnits(2),
  xLarge: miniUnits(1.5),
  large: miniUnits(1.25),
  base: miniUnits(1),
  small: miniUnits(0.5),
  xSmall: miniUnits(0.25),
});